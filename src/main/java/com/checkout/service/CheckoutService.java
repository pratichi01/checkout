package com.checkout.service;

import java.util.List;

public class CheckoutService {

  public void checkoutProducts(List<Integer> productList) {
    //create a product map with id as key and count as value

    // call a repository method to give the
    //detail of each product and their discount info

    //keep on aggregating the values and return the total
  }

}
